module.exports = function(app) {
  //  app.route('/')
		// .get(clickHandler.getAllPolls, function (req, res) {
		// 	res.render("./pages/index", {
		// 		pageTitle : "Home",
		// 		userLoggedIn: req.isAuthenticated(),
		// 		name: (req.user && ("- " + req.user.name.toString())) || "",
		// 		polls: req.allPolls
		// 	});
		// });
};